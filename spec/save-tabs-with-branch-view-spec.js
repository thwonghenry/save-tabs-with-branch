'use babel';

import SaveTabsWithBranchView from '../lib/save-tabs-with-branch-view';

describe('SaveTabsWithBranchView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
